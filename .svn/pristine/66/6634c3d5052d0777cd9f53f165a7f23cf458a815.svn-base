var TabSelect = {
    TabTakeCargo: function (title, index) {
        switch (index) {
            case 1:
                //var tab = $('#tabTakeCargo').tabs('getSelected');
                //$('#tabTakeCargo').tabs('update', {
                //    tab: tab,
                //    options: {
                //        href: 'AssetSite/Take/ListBarcodeRfid.html'
                //    }
                //});
                break;
            default:
                break;
        }
    }
}